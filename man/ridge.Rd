% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ridge.R
\name{ridge}
\alias{ridge}
\title{Ridge Regression Estimator}
\usage{
ridge(X, y, lambda, standardize = TRUE)
}
\arguments{
\item{X}{A numeric design matrix of dimension \eqn{n \times d}.}

\item{y}{A numeric response vector of length \eqn{n}.}

\item{lambda}{A non-negative regularization parameter.}

\item{standardize}{Logical; if \code{TRUE} (default), the predictors are standardized before fitting, else only X is centered.}
}
\value{
An object of class \code{"ridge"} containing:
\itemize{
\item \code{coefficients} — named vector of regression coefficients including intercept
\item \code{lambda} — regularization parameter
\item \code{fitted.values} — fitted values
\item \code{residuals} — residuals
\item \code{call} — matched function call
}
}
\description{
Computes the ridge regression estimator for a linear model using the
closed-form solution.
Ridge regression estimates regression coefficients by minimizing
\deqn{
\min_{\beta} \; \|y - X\beta\|_2^2 + \lambda \|\beta\|_2^2
}
where \eqn{\lambda \ge 0} is a regularization parameter controlling the
amount of shrinkage applied to the coefficients.
}
\examples{
set.seed(1)
X <- matrix(rnorm(100), 20, 5)
y <- rnorm(20)
#Fit the ridge model
fit <- ridge(X, y, lambda = 1)
#Extract information
fit$coefficients
}
\seealso{
\code{\link[=ridge_path]{ridge_path()}} to compute the ridge solution path for a sequence
of regularization parameters.
}
