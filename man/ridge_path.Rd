% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ridge_path.R
\name{ridge_path}
\alias{ridge_path}
\title{Ridge Coefficient Path}
\usage{
ridge_path(X, y, lambda, standardize = TRUE)
}
\arguments{
\item{X}{A numeric design matrix of dimension \eqn{n \times d}.}

\item{y}{A numeric response vector of length \eqn{n}.}

\item{lambda}{A numeric vector of non-negative regularization parameters.}

\item{standardize}{Logical; if \code{TRUE} (default), the predictors are standardized before fitting, else only \code{X} is centered.}
}
\value{
An object of class \code{"ridge_path"} containing:
\itemize{
\item \code{lambda} — vector of lambda values
\item \code{coefficients} — matrix of coefficients
\item \code{call} — matched function call
}
}
\description{
Computes ridge regression estimates over a sequence of regularization parameters.
}
\examples{
set.seed(1)
X <- matrix(rnorm(50), 10, 5)
y <- rnorm(10)
#Fit the ridge model
ridge_path(X, y, lambda = c(0.1, 1, 10))
}
\seealso{
\code{\link[=ridge]{ridge()}} for fitting a ridge regression model for a single
regularization parameter.
}
