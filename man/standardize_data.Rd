% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize_data.R
\name{standardize_data}
\alias{standardize_data}
\title{Standardize data for LASSO / regression}
\usage{
standardize_data(X, y, center = TRUE, scale = TRUE)
}
\arguments{
\item{X}{A numeric matrix of predictors (n x p).}

\item{y}{A numeric response vector of length n.}

\item{center}{Logical, default TRUE. If TRUE, center the columns of X and y by subtracting the mean.}

\item{scale}{Logical, default TRUE. If TRUE, scale the columns of X to have unit ℓ₂-norm after centering.}
}
\value{
A list containing:
\describe{
\item{X}{The standardized predictor matrix.}
\item{y}{The centered response vector.}
\item{X_mean}{The means of the original columns of X.}
\item{X_scale}{The scaling factors used for each column of X.}
\item{y_mean}{The mean of the original response vector y.}
}
}
\description{
Centers and optionally scales the columns of a predictor matrix X and the response vector y.
This is a necessary preprocessing step for LASSO and other regularized regression methods.
}
\examples{
set.seed(1)
X <- matrix(rnorm(20*5), nrow = 20, ncol = 5)
y <- rnorm(20)
std <- standardize_data(X, y)
colMeans(std$X)       # Should be about 0
colSums(std$X^2)      # Should be 1
mean(std$y)           # Should be about 0
}
