% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lar.R
\name{lar}
\alias{lar}
\title{Fit Least Angle Regression (LAR) Model}
\usage{
lar(X, y, max_steps = ncol(X), standardize = TRUE)
}
\arguments{
\item{X}{Numeric predictor matrix}

\item{y}{Numeric response vector}

\item{max_steps}{Maximum number of LAR steps (default = number of predictors)}

\item{standardize}{Logical, whether to standardize X and y (default = TRUE)}
}
\value{
S3 object of class 'lar_model' containing:
\describe{
\item{beta}{matrix of coefficients, columns = steps along the path}
\item{active_sets}{list of active variables at each step}
}
}
\description{
Computes the full LAR path for predictors X and response y.
Optional standardization is applied internally.
}
\examples{
set.seed(1)
X <- matrix(rnorm(30*5), 30, 5)
beta_true <- c(2, -1.5, 0, 0, 0)
y <- X \%*\% beta_true + rnorm(30, sd=0.5)
fit <- lar(X, y)
fit$beta
fit$active_sets

}
