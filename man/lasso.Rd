% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lasso.R
\name{lasso}
\alias{lasso}
\title{Fit LASSO Model (User-Friendly)}
\usage{
lasso(
  X,
  y,
  n_lambda = 100,
  lambda_min_ratio = 0.01,
  tol = 1e-06,
  max_iter = 1000,
  standardize = TRUE
)
}
\arguments{
\item{X}{Numeric predictor matrix (n x p).}

\item{y}{Numeric response vector of length n.}

\item{n_lambda}{Number of lambda values to compute (default 100).}

\item{lambda_min_ratio}{Smallest lambda / largest lambda (default 0.01).}

\item{tol}{Convergence tolerance for coordinate descent (default 1e-6).}

\item{max_iter}{Maximum iterations for coordinate descent (default 1000).}

\item{standardize}{Logical, whether to standardize X and center y (default TRUE).}
}
\value{
A \code{lasso_model} object with:
\describe{
\item{beta}{Matrix of coefficients (p x n_lambda), each column = β for a lambda.}
\item{lambda_seq}{Vector of lambda values used.}
}
}
\description{
Fits a LASSO regression using coordinate descent for a sequence of lambda values.
Standardizes X and centers y internally by default.
}
\examples{
set.seed(1)
X <- matrix(rnorm(50*5), 50, 5)
y <- rnorm(50)
fit <- lasso(X, y, n_lambda=10)
}
