% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lar_path.R
\name{lar_path}
\alias{lar_path}
\title{Compute LAR coefficient path}
\usage{
lar_path(X, y, max_steps = ncol(X))
}
\arguments{
\item{X}{Numeric predictor matrix (standardized)}

\item{y}{Numeric response vector (centered)}

\item{max_steps}{Maximum number of LAR steps (default ncol(X))}
}
\value{
A list containing:
\describe{
\item{beta_path}{Matrix of coefficient estimates (columns = steps)}
\item{active_sets}{List of active index sets}
}
}
\description{
Computes the full Least Angle Regression (LAR) coefficient path.
}
\examples{
set.seed(1)
X <- matrix(rnorm(30 * 5), 30, 5)
beta_true <- c(2, -1.5, 0, 0, 0)
y <- X \%*\% beta_true + rnorm(30, sd = 0.5)
data_std <- standardize_lar(X, y)
X_std <- data_std$X
y_std <- data_std$y
fit <- lar_path(X_std, y_std)
}
