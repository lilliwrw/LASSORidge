% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lasso_path.R
\name{lasso_path}
\alias{lasso_path}
\title{LASSO Path}
\usage{
lasso_path(
  X,
  y,
  n_lambda = 100,
  lambda_min_ratio = 0.01,
  tol = 1e-06,
  max_iter = 1000
)
}
\arguments{
\item{X}{Numeric matrix of predictors (n x p), standardized.}

\item{y}{Numeric response vector, centered.}

\item{n_lambda}{Number of lambda values to compute, default 100.}

\item{lambda_min_ratio}{Smallest lambda / largest lambda, default 0.01.}

\item{tol}{Convergence tolerance for coordinate descent, default 1e-6.}

\item{max_iter}{Maximum iterations for coordinate descent, default 1000.}
}
\value{
A list with:
\describe{
\item{lambda_seq}{Lambda sequence evaluated.}
\item{beta}{Matrix of coefficients (p x n_lambda), each column = β for a lambda.}
}
}
\description{
Computes LASSO estimates for a sequence of lambda values.
}
\examples{
set.seed(1)
X <- matrix(rnorm(50*5), nrow=50)
y <- rnorm(50)
std <- standardize_data(X, y)
path <- lasso_path(std$X, std$y, n_lambda=10)
}
