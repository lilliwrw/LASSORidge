% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.ridge_path.R
\name{plot.ridge_path}
\alias{plot.ridge_path}
\title{Plot Ridge Coefficient Path}
\usage{
\method{plot}{ridge_path}(
  x,
  log.lambda = TRUE,
  legend = TRUE,
  type = "l",
  lty = 1,
  col = NULL,
  xlab = NULL,
  ylab = NULL,
  main = "Ridge Path",
  ...
)
}
\arguments{
\item{x}{An object of class \code{"ridge_path"}.}

\item{log.lambda}{Logical; if \code{TRUE} (default), the horizontal
axis shows \code{log(lambda)}.}

\item{legend}{Logical; if \code{TRUE} (default), a legend is shown in the top right.}

\item{type}{Line type for plotting (default: "l").}

\item{lty}{Line type specification (default: 1).}

\item{col}{Optional vector of colors.}

\item{xlab}{Label for x-axis. If \code{NULL}, a default is chosen.}

\item{ylab}{Label for y-axis. If \code{NULL}, default is "Coefficients".}

\item{main}{Plot title (default: "Ridge Path")}

\item{...}{Additional graphical parameters passed to \code{plot}.}
}
\value{
Produces a plot and returns \code{NULL} invisibly.
}
\description{
Plots the ridge regression coefficient paths as a function
of lambda or log(lambda).
}
\examples{
 set.seed(1)
 X <- matrix(rnorm(100), 20, 5)
 y <- rnorm(20)
 lambda_seq <- exp(seq(-2, 2, length.out = 10))
 path <- ridge_path(X, y, lambda_seq)
 plot(path)
}
