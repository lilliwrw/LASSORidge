% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coef.ridge_path.R
\name{coef.ridge_path}
\alias{coef.ridge_path}
\title{Extract Coefficients from Ridge Path}
\usage{
\method{coef}{ridge_path}(object, step = NULL, ...)
}
\arguments{
\item{object}{An object of class \code{"ridge_path"}.}

\item{step}{Optional lambda value or index.}

\item{...}{Additional arguments (ignored).}
}
\value{
If \code{step} is NULL, the full coefficient matrix.
Otherwise, a coefficient vector for the selected lambda.
}
\description{
Returns the estimated coefficients including the intercepts.
}
\examples{
set.seed(1)
X <- matrix(rnorm(50), 10, 5)
y <- rnorm(10)
path <- ridge_path(X, y, lambda = c(0.1, 1))
coef(path)
}
