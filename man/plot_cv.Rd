% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cv.R
\name{plot_cv}
\alias{plot_cv}
\title{Plot Cross Validation}
\usage{
plot_cv(cv_result, log_scale = TRUE, main = "Cross Validation")
}
\arguments{
\item{log_scale}{Logical, whether to plot log(lambda) on x-axis. Default TRUE.}

\item{main}{Character; main title of the plot. Default is "Cross Validation".}
}
\value{
None (invisible NULL). Produces a plot.
}
\description{
Plots the cv_error for a sequence of lambda values and marks the optimal lambda.
}
\examples{
set.seed(123)
n <- 100; p <- 4
X <- matrix(rnorm(n*p), nrow=n, ncol=p)
y <- rnorm(n)
cv_result <- lambda_CV(X, y, method = "lasso")
plot_cv(cv_result)
}
