% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_lar_path.R
\name{plot_lar_path}
\alias{plot_lar_path}
\title{Plot LAR Coefficient Path}
\usage{
plot_lar_path(
  beta,
  x_axis = c("l1", "step"),
  main = "LAR Coefficient Path",
  xlab = NULL,
  ylab = "Coefficients",
  col = NULL
)
}
\arguments{
\item{beta}{Numeric matrix of coefficients. Rows represent variables,
columns represent steps along the LAR path.}

\item{x_axis}{Character, either "l1" (default) for L1-norm of beta or "step" for step index.}

\item{main}{Plot title (default "LAR Coefficient Path").}

\item{xlab}{Label for x-axis. If NULL, automatically set based on \code{x_axis}.}

\item{ylab}{Label for y-axis (default "Coefficients").}

\item{col}{Optional vector of colors, one per variable. Default uses \code{rainbow()}.}
}
\value{
Invisibly returns NULL
}
\description{
Plots the coefficient path of a Least Angle Regression (LAR) model.
Each line represents one predictor. The x-axis can be either
the L1-norm of coefficients or the step index.
}
\examples{
set.seed(1)
X <- matrix(rnorm(30 * 5), 30, 5)
beta_true <- c(2, -1.5, 0, 0, 0)
y <- X \%*\% beta_true + rnorm(30, sd = 0.5)
fit <- lar(X, y)

# Plot using L1-norm on x-axis
plot_lar_path(fit$beta)

# Plot using step index
plot_lar_path(fit$beta, x_axis = "step")
}
