% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.lar_model.R
\name{plot.lar_model}
\alias{plot.lar_model}
\title{Plot method for LAR models}
\usage{
\method{plot}{lar_model}(x, x_axis = c("l1", "step"), main = "LAR Coefficient Path", ...)
}
\arguments{
\item{x}{An object of class \code{"lar_model"}.}

\item{x_axis}{Character, either "l1" (default) for L1-norm of beta or "step" for step index.}

\item{main}{Plot title (default "LAR Coefficient Path").}

\item{...}{Additional arguments passed to \code{plot_lar_path()}.}
}
\value{
Invisibly returns NULL
}
\description{
S3 plot method for objects of class \code{"lar_model"}.
Internally calls \code{plot_lar_path()}.
}
\examples{
set.seed(1)
X <- matrix(rnorm(30 * 5), 30, 5)
beta_true <- c(2, -1.5, 0, 0, 0)
y <- X \%*\% beta_true + rnorm(30, sd = 0.5)
fit <- lar(X, y)

# Plot using default L1-norm
plot(fit)

# Plot using step index
plot(fit, x_axis = "step")
}
